namespace Dami.Gui;

/// <summary>Conversation copy that does not expose implementation routing.</summary>
public static class DirectChatPresentation
{
    /// <summary>No provider-status narration while the first fragment is pending.</summary>
    public const string PENDING_META = "";
}
